import { NextResponse } from "next/server";import { cookies } from "next/headers";import { v2 as cloudinary } from "cloudinary";import { verifySession } from "@/lib/auth";
export async function POST(req:Request){const t=(await cookies()).get("divyatara_admin")?.value;if(!await verifySession(t))return NextResponse.json({error:"Unauthorized"},{status:401});if(!process.env.CLOUDINARY_URL&&!process.env.CLOUDINARY_CLOUD_NAME)return NextResponse.json({error:"Cloudinary is not configured"},{status:503});if(process.env.CLOUDINARY_CLOUD_NAME)cloudinary.config({cloud_name:process.env.CLOUDINARY_CLOUD_NAME,api_key:process.env.CLOUDINARY_API_KEY,api_secret:process.env.CLOUDINARY_API_SECRET});const fd=await req.formData();const f=fd.get("file");if(!(f instanceof File))return NextResponse.json({error:"File required"},{status:400});const data=Buffer.from(await f.arrayBuffer()).toString("base64");const result=await cloudinary.uploader.upload(`data:${f.type};base64,${data}`,{folder:"divyatara",resource_type:"auto"});return NextResponse.json({url:result.secure_url,publicId:result.public_id})}
