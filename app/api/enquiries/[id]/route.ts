import { NextResponse } from "next/server";import { cookies } from "next/headers";import { connectDb,EnquiryModel } from "@/lib/db";import { verifySession } from "@/lib/auth";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){if(!await verifySession((await cookies()).get("divyatara_admin")?.value))return NextResponse.json({error:"Unauthorized"},{status:401});if(!await connectDb())return NextResponse.json({error:"MongoDB not configured"},{status:503});return NextResponse.json(await EnquiryModel.findByIdAndUpdate((await params).id,await req.json(),{new:true}))}
