import { NextResponse } from "next/server";import { cookies } from "next/headers";import { connectDb,TractorModel } from "@/lib/db";import { verifySession } from "@/lib/auth";
async function allowed(){return verifySession((await cookies()).get("divyatara_admin")?.value)}
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){if(!await allowed())return NextResponse.json({error:"Unauthorized"},{status:401});if(!await connectDb())return NextResponse.json({error:"MongoDB not configured"},{status:503});return NextResponse.json(await TractorModel.findByIdAndUpdate((await params).id,await req.json(),{new:true}))}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){if(!await allowed())return NextResponse.json({error:"Unauthorized"},{status:401});if(!await connectDb())return NextResponse.json({error:"MongoDB not configured"},{status:503});await TractorModel.findByIdAndDelete((await params).id);return NextResponse.json({ok:true})}
